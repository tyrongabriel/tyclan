apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-ingress-ts
  namespace: monitoring
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: prometheus-grafana
      port:
        number: 80
  tls:
    - hosts:
        - grafana
